<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.moviestreaming">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieStreaming"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".Activities.VerifyPhoneActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.FavoriteActivity" />
        <activity android:name=".Activities.ShowSearchActivity" />
        <activity android:name=".Activities.SearchActivity" />
        <activity android:name=".Activities.ShowGenreActivity" />
        <activity android:name=".Activities.EpisodesActivity" />
        <activity android:name=".Activities.VideoPlayActivity" />
        <activity android:name=".Activities.ShowDetailsSeriesActivity" />
        <activity android:name=".Activities.ShowDetailMovieActivity" />
        <activity android:name=".Activities.SeriesActivity" />
        <activity android:name=".Activities.TopMovieImdbActivity" />
        <activity android:name=".Activities.GenreActivity" />
        <activity
            android:name=".Activities.HomeActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>